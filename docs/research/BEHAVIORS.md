# Behaviors

- Header and content are static in normal document flow.
- Navigation switches views without reloading.
- Active navigation and profile tab use the source site's blue state.
- Logout opens a local lock screen; credentials never leave the browser.
- Tables retain their desktop column structure and scroll horizontally below 900px.
- New semester scores are visibly marked as simulated and are deterministic.

